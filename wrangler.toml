# Cloudflare Pages Configuration
# 本地开发使用，生产环境在 Cloudflare 控制台配置

name = "hot-news-aggregation"
compatibility_date = "2024-01-01"

# Pages 配置
pages_build_output_dir = ".vercel/output/static"

[vars]
NODE_VERSION = "20"
